# Supabase — project "support@deltaproclean.com's Project" (ref sryywirmhohrdsssujwf)
# This project is SHARED with other apps; Schedule_Portal lives entirely in the
# `schedule_portal` Postgres schema. Add that schema to Supabase → Settings → API
# → "Exposed schemas" so PostgREST serves it.
VITE_SUPABASE_URL=https://sryywirmhohrdsssujwf.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
